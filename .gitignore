# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
server/.env
# ...but the template with no secrets in it should be shared.
!.env.example
!server/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Device logs — adb_logcat.txt reached 71MB and was committed once; keep it out.
*logcat*.txt
adb_logcat.txt

# Legacy local image uploads, superseded by Cloudinary.
server/uploads/
test.txt

# Playwright MCP session artifacts (snapshots, console logs, screenshots).
.playwright-mcp/

# Impeccable transient session state (decision-page answers).
.impeccable/questions/

# Local agent/tool configuration (Claude Code, Codex, installed skills) — not project source.
.claude/
.codex/
.agents/skills/
